Alerted = false;

function Client_GameRefresh(game)
  
	if (not Altered and not WL.IsVersionOrHigher or not WL.IsVersionOrHigher("5.21")) then
		UI.Alert("You must update your app to the latest version to use the Dragon mod");
		Alerted = true;
		end
	
end
